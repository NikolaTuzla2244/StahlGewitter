has_surplus_economy = {
	custom_trigger_tooltip = {
		tooltip = SG_is_surplus
		check_variable = { economy_index > 0 }
	}
}
has_deficit_economy = {
	custom_trigger_tooltip = {
		tooltip = SG_is_deficit
		check_variable = { economy_index < 0 }
	}
}