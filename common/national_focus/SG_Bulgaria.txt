focus_tree = {
	id = SG_bulgaria_demo_focus

	country = {
		factor = 0

		modifier = {
			add = 10
			tag = BUL
		}
	}
	default = no

	focus = {
		id = BUL_judge_romania
		icon = GFX_goal_unknown
		x = 0
		y = 0
		cost = 3
		# search_filters = {}
		
		available_if_capitulated = yes

		ai_will_do = {
			factor = 30
		}
		available = {
			ROM = {
				has_war_with = HUN
			}
		}
		completion_reward = {
			declare_war_on = {
				target = ROM
				type = annex_everything
			}
		}
	}
	focus = {
		id = BUL_invite_ottoman
		icon = GFX_goal_unknown
		x = 0
		y = 1
		relative_position_id = BUL_judge_romania
		cost = 2
		# search_filters = {}
		
		available_if_capitulated = yes
		prerequisite = { focus = BUL_judge_romania }

		ai_will_do = {
			factor = 30
		}
		available = {
		}
		completion_reward = {
			add_to_faction = TUR
		}
	}
}